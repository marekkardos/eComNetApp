meta {
  name: Get Orders - 200 Authorized
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/Orders
  body: none
  auth: inherit
}

tests {
  test("Status is 200", function() {
    expect([200]).to.include(res.status);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
