meta {
  name: Get Orders - 401
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/Orders
  body: none
  auth: none
}

tests {
  test("Status is 401", function() {
    expect([401]).to.include(res.status);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
